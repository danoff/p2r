{
  "name": "p2r",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:data": "node scripts/build.js",
    "dev": "npm run build:data && npm --prefix site run dev",
    "build": "npm run build:data && npm --prefix site run build"
  },
  "dependencies": {
    "yaml": "^2.4.0"
  }
}
